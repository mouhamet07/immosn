package sn.immosn.backend.annonce.data.entity;

/**
 * Type de transaction d'une annonce immobilière (Sprint 4).
 * Définit si le bien est proposé à la vente ou à la location, dès l'annonce.
 */
public enum TypeTransaction {
    VENTE,
    LOCATION
}
